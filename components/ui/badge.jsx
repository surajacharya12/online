// Placeholder Badge component
import React from 'react';

export default function Badge({ children }) {
  return <span style={{padding: '4px 8px', background: '#eee', borderRadius: '4px'}}>{children}</span>;
}
